import 'package:flutter/material.dart';

class PosActivationView extends StatelessWidget {
  const PosActivationView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('POS Activation'),
      ),
    );
  }
}
